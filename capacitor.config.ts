import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'ski.malinov.calino',
  appName: 'Calino',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
